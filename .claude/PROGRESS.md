# Rawaj — session progress log

Read this before starting a new session so nothing gets rebuilt or contradicted.
This is a running log of what's been done, what's real vs. mock, and what's
left — update it as work continues instead of trusting memory alone.

**Project**: Rawaj — an Arabic (RTL) AI-powered social-media marketing SaaS.
- Frontend: `front/` — Angular 21, standalone components, signals, Reactive Forms.
- Backend: `server/Rawaj/` — ASP.NET Core / .NET 10, CQRS via MediatR, EF Core, SQL Server.

See also: `front/.claude/CLAUDE.md` (Angular style rules) and
`front/.claude/README.md` (this project's specific frontend conventions —
error handling, loading states, styling, shared components). Follow both;
this file is the "what happened and what's next" log, not the style guide.

---

## Project structure — respect these conventions

### Frontend (`front/src/app/`)
- `layout/user/` — the tenant dashboard shell (`UserLayout`, `Sidebar`, `Header`), mounted at `/dashboard`.
- `layout/admin/` — the platform-admin shell (`AdminLayout`, `AdminSidebar`, `AdminHeader`, `admin-theme.css`), mounted at `/admin`. Dark-red/white palette lives only in the sidebar/header's own CSS — the content area intentionally uses the normal light theme (see "Admin dashboard" below).
- `features/<domain>/<page-name>/` — one folder per routed page or major sub-component, `.ts`/`.html`/`.css` per component. Dashboard-only pages that aren't part of the shell still live under `features/dashboard/*` (billing, settings, users, notifications, help-support, my-projects, loading-test-page).
- `features/admin/` — admin-only pages (`admin-overview-page`, `admin-users-page`, `admin-tenants-page`, `admin-plans-page`, `admin-settings-page`) + `admin-shared.css` (shared table/search/user-cell styles, mirrors `dashboard-shared.css`).
- `shared/components/` — reusable components used across features (`error-modal`, `modal-shell`, `page-loader`, `file-upload`, `faq-section`, `breadcrumb`, `page-header`).
- `shared/directives/` — `tooltip.directive.ts` (`appTooltip`), `gsap-reveal.directive.ts` (`appGsapReveal`).
- `core/auth/` — `AuthService`, `jwt.util.ts` (decode access token), `api-error.util.ts` (map failed HTTP responses to modal/form errors).
- `core/tenant/` — `TenantService` (real, HTTP-backed — replaced the old mock `services/tenant.service.ts`): current tenant summary signal (`coinBalance`, `isActivated`, `isAgency`, `defaultBrandProfileId`, plan/profile fields) **plus** (item 19) `memberships`/`activeTenantId`/`switchTenant()`/`isOwnTenantActivated` for multi-tenant membership.
- `core/social/` — `SocialAccountService` (real, HTTP-backed): list/connect/disconnect social accounts for a brand profile via the backend's redirect-based OAuth flow.
- `core/guards/` — `auth.guard.ts`, `admin.guard.ts`, `guest.guard.ts`, `activation.guard.ts` (item 19 — gates pages behind the caller's own-tenant activation) (functional guards, `CanActivateFn`/`CanMatchFn`).
- `core/interceptors/` — `auth.interceptor.ts` (attaches bearer token, one silent refresh-and-retry on 401), `tenant.interceptor.ts` (attaches `X-Tenant-Id: activeTenantId`, item 19).
- `model/` — plain interfaces/types, one file per domain (`auth.model.ts`, `tenant.model.ts`, `social-account.model.ts`, `campaign.model.ts`, `brand-profile.model.ts`, `admin.model.ts`, `generated-item.model.ts`, `team-member.model.ts`, etc.). Auth/tenant/social-account/team-member model types are named to mirror the backend's C# records exactly (see below).
- `services/` — several are still **mock data stores**: `private signal<T[]>` + `.asReadonly()` + `getById()` computed + plain mutation methods (`AdService`, `MediaService`, `AdminService`). Real, HTTP-backed: `AuthService`/`TenantService`/`SocialAccountService` (in `core/`), `BrandProfileService`, `CampaignService`, and (item 19) `TeamMemberService`.
- `routes/` — `user.routes.ts` (children of `/dashboard`) and `admin.routes.ts` (children of `/admin`), composed by `app.routes.ts`.
- `environments/` — `environment.ts`/`.development.ts`/`.production.ts`, holds `apiUrl` (`https://localhost:7206/api/v1` in dev). Any new env-specific value goes here, in all three files.

### Backend (`server/Rawaj/`)
Layered/CQRS solution:
- `Rawaj.Domain` — entities, enums, value objects. No dependencies on other layers.
- `Rawaj.Application` — `Features/<Module>/<Action>/` folders, each holding a MediatR `Command`/`Query` + `Handler` + `Validator` (FluentValidation) + `Response` record. Common interfaces/models/policies live in `Application/Common/`.
- `Rawaj.Persistence` — `AppDbContext`, EF Core configurations, migrations, and the concrete implementations of `Application`'s interfaces (`IdentityService`, `BrandProfileService`, etc.).
- `Rawaj.Infrastructure` — cross-cutting concerns: JWT generation, encryption, storage, background jobs, external API clients.
- `Rawaj/` (API project) — `Controllers/*Controller.cs` (thin, just `sender.Send(command)` + map `Result` to `ApiResponse<T>`), `Program.cs`, `appsettings*.json`.
- Every controller action returns `Rawaj.Common.ApiResponse<T>`: `{ status: "success"|"fail"|"error", data, message, errors }` — `errors` is a `Dictionary<string,string[]>` (FluentValidation field errors), only populated on 400s.
- New backend work should follow this exact pattern (new `Features/<Module>/<Action>/` folder with all 4 files) — don't put logic directly in controllers.

---

## What's been built so far (chronological, high level)

1. **Ads/posts, campaigns**: renamed ads → "منشوراتك", redesigned `ad-card`/`campaign-card`, added campaign detail → calendar → post-detail page flow (real routes, no modals), shared mock services (`CampaignService`, `ScheduledPostService`, `AdService`).
2. **Landing page**: rebuilt "problems" section styling, hero/navbar glow tuning (dark-red/magenta palette, `--shadow-tag` design token).
3. **Shared infra**: `ErrorModalService` + `error-modal` component (built on `ModalShell`), `LoaderService` + `page-loader` (global, wired to router nav events, previewable at `/dashboard/loading-test`), 404 page, environment config files, `TooltipDirective` (`appTooltip`), generalized `FileUpload` component, redesigned `FaqSection` component.
4. **Users table / billing**: replaced role/department columns with project-count/credit-used, added ban/reactivate action, generalized the `--shadow-tag` box-shadow across badges/avatars/icons.
5. **Brand profiles**: `BrandProfile` model (mirrors backend `TenantBrandProfileResponse`), mock `BrandProfileService`, list page (tags-top/big-image/data-bottom/actions card), multi-step creation **page** (not modal) with progress bar, `Campaign.brandProfileId` linking campaigns to a brand profile.
6. **Settings**: social-account connections moved from a dashboard quick-action into Settings (agency accounts connect per brand profile via a new `TenantService`; business accounts share one connection set); removed the "المظهر" (appearance) tab.
7. **Content generation page**: reworked to a single output preview (placeholder → generating animation → result) instead of a full gallery, "edit with prompt" flow, assets upload with name+thumbnail previews, brand-profile/campaign tagging feeding new filters on the my-media library page.
8. **Layout reorg**: moved the dashboard shell out of `features/dashboard/` into `layout/user/` (renamed `Dashboard` → `UserLayout`); routes split into `routes/user.routes.ts` + `routes/admin.routes.ts`.
9. **Admin dashboard** (`/admin`, platform-wide, distinct from the tenant `/dashboard`): `AdminLayout`/`AdminSidebar`/`AdminHeader` (dark red + white text, hardcoded in their own CSS — **not** via the removed `.admin-shell` variable-override approach, which was tried and reverted because it also darkened the content area), pages for overview stats / cross-tenant users / tenants (agencies vs. business owners) / subscription plans / general settings, backed by a **mock** `AdminService`. The backend already has real equivalents (`Features/Admin/GetPlatformStats`, `GetUsers`, `GetTenants`, `SetUserActive`) that aren't wired up yet — see "Next steps."
10. **Real auth integration** (this is the first module connected to the live backend, not mock):
    - Fixed/confirmed `RawajDb` as the target database (both `appsettings.json` and `appsettings.Development.json` point to it); ran `dotnet ef database update` to create it locally (9 migrations applied).
    - Registered a real platform-admin account: `admin@gmail.com` / `Admin@123456789`, then set `IsPlatformAdmin = 1` directly via SQL (no API command exists for this yet).
    - Verified register → login → refresh-token → logout all work live against the local DB; confirmed the JWT's `platform_admin` claim reads `true` for that account.
    - Frontend: `model/auth.model.ts`, `core/auth/auth.service.ts` (+`jwt.util.ts`, `api-error.util.ts`), `core/interceptors/auth.interceptor.ts`, `core/guards/{auth,admin,guest}.guard.ts`, wired into `app.config.ts` (`provideHttpClient(withInterceptors(...))`) and `app.routes.ts` (`canActivate`/`canMatch` on `/dashboard`, `/admin`, `/account-setup`, `/on-boarding`, `guestGuard` on `/login`/`/sign-up`).
    - `login-form`/`sign-up-form` now call the real `AuthService`, show errors via `ErrorModalService`, map server field errors onto the form (`FormErrorsService` extended with a `serverMessage` error key), use `LoaderService` while in flight. Login's field was `emailOrUserName`; corrected to a proper `email` field since the backend only supports email login.
11. **Real tenant lifecycle** (auto-provisioning, coins, activation reward, agency upgrade, social OAuth — all real, not mock):
    - Backend: `Tenant` gained `CoinBalance` (int), `IsActivated` (bool), and a `TenantProfile` value object (JSON column, same pattern as `BrandInfo`: `Phone`, `Industry`, `Country`, `City`, `Website`, `AgencySize`, `ServicesOffered`) — migration `AddTenantCoinsActivationProfile`.
    - `RegisterCommandHandler` now auto-provisions a `Tenant` (`TenantType.Business`, 100 starting coins, `IsActivated=false`), a `Subscription` (Free plan, 14-day trial), a `TenantMember` (Owner), and **one default `TenantBrandProfile`** (`IsDefault=true`) — all via a new shared `Application/Common/Services/TenantProvisioningService`, also reused by the pre-existing (previously unused) `CreateTenantCommandHandler`.
    - `GetMyTenantQuery`/`Response` extended with `coinBalance`, `isActivated`, `planName`, `maxBrands`, `brandProfileCount`, `defaultBrandProfileId`, and the `TenantProfile` fields.
    - New `Features/Tenants/UpdateTenantProfile` (`PUT /tenants/me/profile`): merges the given fields into `TenantProfile`; when `Phone`+`Industry`+`Country`+`City` are all non-empty for the first time, flips `IsActivated=true` and grants **+50 coins** (`activationRewardGranted` in the response, guarded so it never re-grants).
    - New `Features/Tenants/UpgradeToAgency` (`POST /tenants/me/upgrade-to-agency`): requires `AgencySize`+`ServicesOffered` (Owner role only), fails if already `Agency`, otherwise sets `TenantType=Agency`, cancels the current `Subscription` and creates a new one on the **Pro** plan (already-seeded, `MaxBrands=5`) — no new plan entity needed.
    - `CreateBrandProfileCommandHandler`'s plan-limit failure message is now the exact sentinel string `"AGENCY_UPGRADE_REQUIRED"` (was a prose message) so the frontend can reliably detect "you need to upgrade" vs. any other failure. `BrandInfo`/`CreateBrandProfileCommand` also gained an optional `Location` field (JSON column, no migration needed).
    - All of the above verified live end-to-end via curl against `RawajDb` (register → tenant+default brand profile created with 100 coins → partial profile update doesn't reward → completing it rewards +50 coins once, not twice → blocked 2nd brand profile creation returns the sentinel → upgrade-to-agency flips type/plan → 2nd brand profile creation then succeeds).
    - Frontend: `core/tenant/tenant.service.ts` (replaces the old mock `services/tenant.service.ts`, same `isAgency` computed name so `settings-page` needed only an import-path change), `model/tenant.model.ts`. `login-form`/`sign-up-form` call `tenantService.refresh()` on success; `UserLayout`'s constructor also calls it once if the tenant signal is empty (covers page reload/deep-link). Sign-up now navigates straight to `/dashboard` (no more forced wizard) and dropped its now-dead `userName`/`businessName` fields.
    - `/account-setup` → renamed to `/upgrade-tenant`: the whole `features/account-setup/` folder was renamed to `features/upgrade-tenant/`, `setup-type-selector`/`business/*`/`otp-verification` deleted (a brand-new signup no longer needs to choose a type or verify OTP — that's all automatic now), keeping only the 5 agency steps + `setup-stepper`. The page now calls the real `upgradeToAgency()` and redirects into `/dashboard/brand-profiles/new` on success.
    - Dashboard home (`crm-page`) gained two quick-action cards: "اربط حسابات التواصل الاجتماعي" (→ new `/dashboard/social-accounts`) and "أكمل بياناتك واحصل على 50 كوين" (→ Settings profile tab; hides itself once `isActivated()`).
    - New `features/dashboard/social-accounts-page/` (real): lists/connects/disconnects social accounts for the tenant's default brand profile via `SocialAccountService`, using the backend's existing redirect-based OAuth flow (`GetAuthorizationUrl` → full-page redirect → backend's `Callback` action redirects back to `/dashboard/social-accounts?connected=...`/`?error=...`).
    - Settings profile tab gained a second card ("بيانات النشاط التجاري": phone/industry/country/city/website) wired to `tenantService.updateProfile()`, with a success modal on activation reward.
    - Billing page now shows the real `planName`/`coinBalance` from `TenantService` (invoices/usage bars stay mock).
    - Brand-profile creation page: added an optional `Location` field with the requested Arabic tip ("هذه المعلومة سوف تساعد رواج في تحليل المنافسين"); since brand-profiles remain a **mock** module this pass, the "upgrade required" gate on submit checks the mock service's local profile count + `tenantService.isAgency()` client-side (not the backend sentinel yet — that only takes effect once brand-profiles are wired to real HTTP).
12. **Brand profiles wired to real backend** (no longer mock):
    - `services/brand-profile.service.ts` now hits the live API — `refresh()` (`GET /brand-profiles`, maps `BrandProfileSummary` → `BrandProfile`), `create()` (`POST /brand-profiles`), `archive()` (`POST /brand-profiles/{id}/archive`), all funneled through a `mutateAndRefresh` helper that re-fetches the list on success so the `profiles` signal always mirrors server state. The old local `signal<BrandProfile[]>` mock store is gone.
    - The create wizard's "upgrade required" gate now reads the backend's real state via `tenantService` (`brandProfileCount()` / `maxBrands()` / `isAgency()`) instead of a client-side mock count.
13. **Brand-profile requirement gating** (new tenants start with **zero** brand profiles):
    - Backend: `RegisterCommandHandler` now provisions with `createDefaultBrandProfile: false` — a fresh signup gets a tenant + subscription + Owner member but **no** brand profile (reverses item 11's "one default `TenantBrandProfile`"). `CreateTenantCommandHandler` is untouched.
    - Frontend rule: navigation and page content stay **fully unrestricted** — every sidebar item is clickable, every route reachable, every page renders its normal layout/lists/cards. Only the brand-*dependent actions* are gated when `tenantService.brandProfileCount() === 0`, reusing the existing brand-cap pattern from `brand-profile-create-page.ts` inline (an `ErrorModalService.show(...)` warning + `router.navigate(['/dashboard/brand-profiles/new'])`) — no new shared component/directive.
    - Gated actions: campaigns-page (`startNewCampaign`, pause/resume), ads-page + ad-detail-page (`toggleStatus`), calendar-page (`savePost`/`deletePost`), marketing-plan-page (`goToOnboarding`/`remake`). Read-only detail/my-media pages were left alone (no mutating action to gate).
    - `crm-page` shows a "أنشئ ملف علامتك التجارية أولاً" CTA banner (reusing the reward-card style) while `brandProfileCount() === 0`; the header brand selector gains a third branch rendering an "أنشئ ملف علامة تجارية" link when the profiles list is empty.
    - **Content Generation is the sole exception** — it stays fully usable with zero brand profiles (already defaulted its brand id to `''`, no change needed).
14. **Brand-profile logos stored as files, not base64** (mirrors the user-avatar mechanism):
    - Previously the create wizard embedded the logo as a base64 `data:` URL in `BrandInfo.LogoUrl`. Now the file uploads to disk and only a short relative path is persisted.
    - Backend: generalized the avatar storage — `IAvatarStorageService`/`LocalAvatarStorageService` → `ILocalImageStorageService`/`LocalImageStorageService` with a `subfolder` param (`SaveAsync`/`DeleteAsync`; avatars keep their existing on-disk location); the two avatar handlers + DI registration were updated. New `Features/Brands/UploadBrandLogo/` (command/handler/response/validator, Admin-only, 5 MB cap, jpeg/png/webp/**svg**) + `POST /brand-profiles/logo` (accepts `IFormFile`, saves to `wwwroot/media/brandprofiles/logourls/`, returns `/media/brandprofiles/logourls/{file}`). The `LogoUrl` rule in the Create/Update brand-profile validators was relaxed to accept root-relative `/media/` paths (it required an absolute URI, which the new path would have failed; legacy `data:` URLs still pass).
    - Frontend: `BrandProfileService.uploadLogo(file)` (posts `FormData`), and `toBrandProfile` now resolves `logoUrl` through `resolveMediaUrl` (so `/media/...` paths resolve to the API origin in dev; legacy base64 values pass through unchanged). The wizard keeps the data URL for preview only, captures the real `File`, and on submit uploads it first then creates the profile with the returned path.
    - **Onboarding wizard's logo is out of scope** — it only ever stored the file *name*, never binary, so it wasn't touched.
15. **Dead auth nav links fixed**: the login page's "إنشاء حساب" link and the sign-up page's "تسجيل الدخول" link were `<a href="#">` placeholders with no routing. Added `RouterLink` to both standalone form components' `imports` and pointed them at `/sign-up` and `/login`.
16. **Celebration modal fixed**: the confetti/checkmark never rendered because a `queueMicrotask` read `viewChild()` signals before Angular had mounted the projected content. Rewritten to read the `viewChild()` signals directly inside `effect()`, so Angular's dependency tracking re-fires once the refs actually resolve.
17. **Settings business-info validation**: full frontend+backend validation added for phone/industry/country/city (maxlength) and website (a lenient URL validator on the frontend matching the backend's `UrlNormalizer` exactly), with inline errors mirroring the backend rules.
18. **Real welcome email**: a real Gmail-SMTP `IEmailService`/`SmtpEmailService` (`Rawaj.Infrastructure/Email/`) sends a welcome email on registration. Deliverability measures within the app's control: a hand-written plain-text `AlternateView` alongside the HTML (not auto-stripped), a `List-Unsubscribe` header, table-based RTL layout. **Caveat that remains true**: a personal Gmail SMTP account has no SPF/DKIM/DMARC on a real sending domain, so it will often still land in spam regardless of template quality — the only true fix is sending from a real domain with DKIM configured.
19. **Team collaboration / multi-tenant membership / coin wallets / email invites — now real, end to end** (backend + frontend, the largest module to date):
    - **The `X-Tenant-Id` contract**: every registered user auto-provisions their own tenant, so an invited member always belongs to ≥2 tenants. The frontend's `tenantInterceptor` attaches `X-Tenant-Id: <activeTenantId>` to every request; the backend's `TenantResolutionPolicy.ResolveAsync` (used identically by `TenantAuthorizationBehavior` and `GetMyTenantQueryHandler` so they can never diverge) picks: the requested tenant if the caller has an **Accepted** membership in it → else the tenant they own → else their first accepted membership. Requesting a tenant the caller isn't an accepted member of throws `ForbiddenAccessException` (403), never silently falls back. `GET /tenants/memberships` lists every tenant a user belongs to with `myCoinBalance` (see below) — the frontend's `TenantService` tracks `memberships`/`activeTenantId` (persisted to `localStorage`), exposes `switchTenant(id)`, and `isOwnTenantActivated` (finds the membership where `isOwner` is true, independent of whichever tenant is currently active).
    - **The coin escrow model**: `Tenant.CoinBalance` is the pool, spent directly by Owner/Admin. `TenantMember.AllocatedCoins`/`SpentCoins` is a per-member wallet for Editor/Viewer, **escrowed out of the pool immediately** when a member is invited or re-allocated (`AllocateCoinsCommand`, Admin+, sets an absolute new allocation and moves the delta). `CoinPolicy.GetBalanceAsync`/`TrySpendAsync` (`Application/Common/Policies/`) is the single shared policy for both reading and spending — Owner/Admin read/spend `Tenant.CoinBalance`, everyone else reads/spends their own wallet. Removing a member, declining an invite, or revoking a pending invitation all return any **unspent** allocation to the tenant pool (audited in three separate code paths so none of them leak coins). Coin costs (`Coins` appsettings section, defaults 5/10/20/30) are charged on generation **success only** (never on failure) for content/visual/campaign-content/marketing-plan generation, via `ICoinCostProvider`.
    - **Two invite paths**: an email that already has a Rawaj account gets a Pending `TenantMember` (its `Id` doubles as the invite-link token, since the existing accept/decline endpoints already verify `m.UserId == caller`); a brand-new email gets a `TenantInvitation` row with a SHA-256-hashed, 7-day-expiry random token (mirrors `RefreshTokenPolicy`'s hashing pattern). `GetInvitationDetailsQuery` is anonymous and tries both interpretations (`Guid.TryParse` first, hash lookup as fallback) so the frontend's `/invite` page needs exactly one lookup call regardless of invite kind, and returns `requiresRegistration` + the inviter's name + tenant name (per the requirement that the invite page names who invited you). `AcceptInvitationAndRegisterCommand` is a one-shot anonymous register-and-join: creates the user, provisions their own tenant (unactivated, 100 coins, no default brand profile), creates an Accepted `TenantMember` with the pre-escrowed coins, and returns JWT+refresh tokens so the invitee lands logged in immediately.
    - **Activation gating**: `activationGuard` (`core/guards/activation.guard.ts`) redirects to `/dashboard/locked` (an Arabic "أكمل بيانات حسابك" placeholder with a CTA into Settings) whenever `tenantService.isOwnTenantActivated()` is false. Per an explicit product decision, it's applied to `brand-profiles(/new)`, `campaigns(/:id, /:id/calendar, /:id/posts/:postId)`, `calendar`, `marketing-plan`, `users(/:id)`, `social-accounts`, and `billing` — and deliberately **left unguarded** so an unactivated invitee can still work inside the tenant that invited them: `content-gen`, `ads(/:id)`, `my-media`, `my-projects`, `notifications`, `settings`, `help`, and the dashboard home.
    - **Audit log / activity feed**: a new `AuditLogger` policy (mirrors `NotificationPublisher`'s "add to context, caller saves" pattern) writes `team.*`-prefixed actions (invite sent/accepted/declined/revoked, member removed, role changed, brand access changed, coins allocated) against the newly tenant-scoped `AuditLog.TenantId`. `GetTeamActivityQuery` (`GET /team-members/activity`, paginated, optional `?userId=` filter) backs both the users-page's new **السجل** (logs) tab and a compact per-member activity list on the profile page.
    - **Frontend team module, fully rewritten from mock to real**: `model/team-member.model.ts` and `services/team-member.service.ts` now mirror the backend exactly (roles `Owner|Admin|Editor|Viewer`, status `Pending|Accepted|Declined`, `allocatedCoins`/`spentCoins`/`brandProfileIds`) — the old invented `department`, permission-matrix, `creditUsage`, and `lastPasswordResetAt` fields are gone. `users-page` gained تبويبات (الكل/نشط/بانتظار القبول/السجل), a coins column, and unifies real `TenantMember` rows with no-account `TenantInvitation` rows in one table. The invite modal (`user-form-modal`) now collects email/role/brand-profile multi-select/allocated-coins. `user-profile-page` was rebuilt to show role (inline change), status, join date, assigned brand profiles (via the repurposed `assign-projects-modal`), allocated-vs-spent coins (with an inline re-allocate editor), and that member's activity log — **`reset-password-modal/` was deleted entirely**, since an invited user is a full account owner, not a password-manageable sub-account. `features/auth/invite-login/` was rewritten for the real flow: shows "{inviter} دعاك للانضمام إلى {tenant}", branches to a registration form (`invite-form`, brand-new email) or a sign-in-then-accept prompt (existing account), and explicitly guards against an already-signed-in *different* account silently accepting someone else's invite (`emailMismatch`).
    - **Two coin balances, kept explicitly distinct**: the header's tenant/organization switcher (only rendered when `memberships().length > 1`) shows each tenant's name and *that membership's own* `myCoinBalance` — never the tenant owner's full pool. The same real balance now backs the header's credit display (replacing a `signal(2450)` mock). `content-gen-page` gained a wallet selector (same `showWalletSelector`/`memberships` pattern) so an invited member picks — explicitly, "since it is a credit usage" — which tenant's wallet a generation will charge, switching brand-profile/campaign lists to match.
    - **`my-projects-page` rewritten**: it now shows the real brand profiles (and their campaigns) accessible in the active tenant instead of a mock `TeamProject` list — no client-side filtering needed since the backend already scopes brand-profile/campaign visibility to the caller's brand-access grants.
    - **UI fixes bundled with this module**: header tooltips (`sidebar-toggle-btn`, `mobile-menu-btn`, fullscreen button) now pass `position="bottom"` so they open downward instead of clipping above the header; "إنشاء ملف علامة تجارية" (`brand-profiles-page.html`, both the header and empty-state copies) now uses `rw-btn--primary` (secondary-color gradient, white text) instead of `rw-btn--dark`; the users-page's horizontal scrollbar (`users-table-wrap`) is now responsive-only (`overflow: visible` at desktop, `overflow-x: auto` only under `max-width: 992px`) — it was showing permanently because the row-action tooltip `::after` boxes inflated `scrollWidth` even at desktop width where nothing visually overflows.
    - Backend: 48/48 tests pass (10 new: `CoinPolicyTests`, `TenantResolutionPolicyTests`). Verified live via curl end-to-end: invite → escrow confirmed on the tenant pool → anonymous token lookup → register-and-accept → invitee shows exactly 2 memberships with correct per-membership `myCoinBalance` → `X-Tenant-Id` switching confirmed (including 403 on a foreign tenant) → `GET /tenants/me`'s `coinBalance` confirmed to show the CALLER's own balance after switching into the inviting tenant (see the coin-leak bug below). `ng build` passes clean (only pre-existing bundle-budget warnings, no errors).
    - **Two real bugs caught and fixed via live testing, not user reports**: (1) `SmtpEmailService` crashed registration entirely because `appsettings.json`'s placeholder `SenderEmail` threw from `new MailAddress(...)` **outside** the try/catch — fixed by validating via `MailAddress.TryCreate` and wrapping the entire method body. (2) `GetMyTenantQueryHandler` leaked the tenant owner's full coin pool to an Editor who switched into that tenant (it returned the raw `Tenant.CoinBalance` for every role) — fixed by routing `coinBalance` through `CoinPolicy.GetBalanceAsync` instead.
20. **Subscription/coin economy overhaul — real pricing, fake payments, reward celebration** (backend + frontend):
    - **Four real subscription tiers replace the old placeholder Free/Pro**: `SubscriptionPlanConfiguration` now seeds Free ($0, 1 brand, 0% discount), Plus ($99, 10 brands, 10%), Pro ($249, 30 brands, 18%), Ultra ($599, 100 brands, 30%) — `SubscriptionPlan.CoinUsageDiscountPercent` is new. `MaxUsers` on each plan = the pricing sheet's "marketeers" + 1 (the owner already occupies a seat), so Free's 0 marketeers ⇒ `MaxUsers=1` (invites blocked on Free, matching the sheet exactly).
    - **Real coin price list**: `ICoinCostProvider`/`CoinCostSettings` (`Coins` appsettings section) now carries the real per-action prices — `ContentGeneration=200` (single social post), `VisualGeneration=400` (single AI image), `CampaignContentGeneration=1000` (bundled batch, still an approximation per the existing code comment), `MarketingPlanGeneration=12000` (complete strategy), and a new `Scheduling=100` (schedule one post/story/reel/carousel/promo — newly wired into `SchedulePostCommandHandler`, the first real coin charge on scheduling). Three items from the pricing sheet — `BusinessDiagnosis`, `CompetitiveAnalysis`, `ReasoningConversation` — are **reserved pricing only** (exposed for display, no feature spends them yet; explicit scope decision, not an oversight).
    - **Per-plan discount**: new `Common/Policies/CoinPricingPolicy.GetDiscountedCostAsync` applies the tenant's plan `CoinUsageDiscountPercent` to a base cost (`ceil(base * (1 - pct/100))`); all 5 AI handlers plus scheduling call this once before their existing `CoinPolicy` balance check/spend — no other change to those handlers' shape.
    - **New-tenant free trials** (pricing sheet §3): `Tenant` gained `FreeMarketingPlanUsed` (bool, one-time), `FreeImageGenerationsRemaining`/`FreeContentGenerationsRemaining` (int, seeded to 5 by `TenantProvisioningService`). `GenerateMarketingPlanCommandHandler`/`GenerateVisualAssetCommandHandler`/`GenerateContentItemCommandHandler` check/consume these **before** any coin math, only on generation success — `RegenerateContentItem` deliberately does **not** draw on the free quota (would let editing farm free generations).
    - **Add-ons**: `Tenant.ExtraBrandsPurchased`/`ExtraMarketeersPurchased` (new columns) add on top of the plan's `MaxBrands`/`MaxUsers` wherever those limits are checked (`CreateBrandProfileCommandHandler`, `AddTeamMemberCommandHandler`). New `PurchaseAddOnCommand` (Owner-only, $5/extra brand, $3/extra marketeer) — a one-time "unlock" since there's no recurring billing/webhook infrastructure to re-charge monthly yet.
    - **Fake payment, everywhere real money would flow**: no gateway call, no card validation, always succeeds — new `Rawaj.Domain.Entities.Billing.BillingTransaction` (Type `CoinPurchase|PlanChange|AddOnPurchase`) records every one, backing a real billing-history list. New `PurchaseCoinsCommand` (buys a `CoinPackage` — Starter/Growth/Business/Enterprise, seeded exactly per the pricing sheet — or a custom amount at the Starter flat rate `$0.01/coin`). `ChangeSubscriptionPlanCommand` (already real/generic pre-existing) extended to optionally take `AgencySize`/`ServicesOffered` (only required the first time a tenant leaves Free) and flip `TenantType→Agency` when the target plan costs more than $0 — this is now the one real subscribe/upgrade/downgrade endpoint the frontend calls; the older hardcoded-"Pro" `UpgradeToAgencyCommand` was **deliberately left in place** (still used by the existing onboarding wizard) rather than retired, to avoid touching a working, unrelated flow — a scope trim from the original plan.
    - **Two new anonymous endpoints** so pricing is visible before login: `GET /subscriptions/plans` (existing query, now `[AllowAnonymous]`) and new `GET /subscriptions/public-coin-pricing` (base costs + coin packages + add-on prices, no per-tenant discount/free-trial state — those need a signed-in tenant). Both back the landing page's pricing teaser and the new public `/pricing` page identically, so numbers can never drift between them or from what `dashboard/billing` will actually charge.
    - **Frontend**: `model/billing.model.ts` + `services/subscription.service.ts` + `services/coin-pricing.service.ts` (real HTTP, mirroring `TenantService`'s shape). `billing-page` fully rebuilt — real plan card with a working plan-picker (agency-info fields only shown when leaving Free) and cancel button, real AI-credit usage bar, real coin-package/custom-amount purchase UI, add-on purchase UI, real transaction-history table — replacing every hardcoded `UsageMetric`/`Invoice`/dead button from before. New global `FakePaymentModalService`/`<app-fake-payment-modal>` (mounted in `app.html` alongside the existing error/confirm/celebration modals) is the one confirm-and-pay dialog every purchase in the app goes through. New public `features/pricing/pricing-page/` (route `/pricing`, no guard) explains all four tiers, all five coin packages, both add-ons, and the full AI price list (reserved items marked "قريبًا"); the landing page's pricing teaser (`features/landing/pricing/`) was simplified from its old 3-column asymmetric layout (annual toggle, fake $ figures) to a real 4-tier equal grid, monthly-only, linking out to `/pricing` for the rest.
    - **Spend-preview captions**: `content-gen-page` shows a real "سيتم خصم N كوين من رصيدك" (or "توليد مجاني، متبقي N" while free-trial quota remains) caption under its generate button, sourced from `coin-pricing.service.ts`'s cached response — the only concrete, easy-to-locate button this was wired to this pass (see gap below).
    - **Celebration modal**: `front/public/assets/animations/Celebrations Begin.svg` added as a low-opacity absolutely-positioned background layer behind the existing confetti/checkmark (`celebration-modal.html`/`.css`, no service-shape change). New trigger point: the registration +100-starting-coins bonus, fired once right after a successful sign-up (`sign-up-form.ts`) — on top of the pre-existing tenant-activation trigger, every purchase/plan-change success in `billing-page.ts` now also triggers it.
    - **Invite-email "not working" investigated, no code bug found**: `appsettings.Development.json`'s `Email:SenderEmail`/`SenderPassword` were simply empty (despite believing they'd been filled in) — `SmtpEmailService` correctly no-ops when blank, so the invite record/coin-escrow/notification/audit-log all still succeed but no email is attempted. Gmail requires a 16-character **App Password** (Google Account → Security → 2-Step Verification → App passwords), never the normal account password — documented directly in `EmailSettings.cs`'s doc comment now. New `IEmailService.IsConfigured` + `AddTeamMemberResponse.EmailConfigured` surface this to the inviter as an inline warning instead of the invite silently looking identical whether or not mail actually went out.
    - **Settings business-info "not saving" investigated, no code bug found**: traced `saveBusinessProfile()` → `TenantService.updateProfile()` → `UpdateTenantProfileCommandHandler` → `SaveChangesAsync`, and confirmed `TenantProfile`'s EF `HasJsonConversion()` already installs a correct `ValueComparer` (a real JSON-round-trip snapshot clone, not reference-identity) so in-place mutation of the JSON column **is** correctly change-tracked. Far more likely explanation: this was tested while the DB connection string was still the broken placeholder (see the fixed known-issue below) — every write, this one included, would have silently failed while the Angular form still *looked* saved (reactive-form state). Live-verified during this pass: activating a fresh tenant via `PUT /tenants/me/profile` correctly persisted and granted the +50 coin reward.
    - Backend: 48/48 tests still pass (no new ones added this pass — all new logic is either straight EF/config plumbing or follows the exact tested pattern of the 5 existing coin-spend handlers). Verified live via curl end-to-end on a fresh tenant: anonymous `plans`/`public-coin-pricing` work with no `Authorization` header; register → tenant shows `coinBalance:100, maxUsers:1` (Free) → activate profile → `+50` reward coins persisted correctly (see settings-bug note above) → buy Starter package (+5000) → buy 250 custom coins (+250, $2.50) → buy an extra-brand add-on (`extraBrandsPurchased:1`) → change plan to Plus with agency info → tenant flips to `tenantType:Agency, planName:Plus, maxUsers:9` and billing history shows all 4 transactions newest-first with correct amounts → generating content on a fresh tenant correctly attempted the free-trial path and (failing only on the unrelated missing Groq API key in this dev environment) confirmed neither coins nor the free-trial counter were touched on failure. `ng build` clean (only the pre-existing bundle-budget warnings).
    - **A real config-drift bug caught via this live testing**: `appsettings.Development.json` (gitignored, local-only) had its own stale `Coins` section with the old 5/10/20/30 values, silently overriding the new prices in `appsettings.json` — since Development-environment config always wins. Fixed by updating both files' `Coins` sections in lockstep; worth remembering that **any** future appsettings.json change needs the same mirrored update in the local Development file to actually take effect when running via `dotnet run` in Development.
    - **Known gap, by design this pass**: `content-gen-page`'s "generate" button (and `marketing-plan-page`'s, and the calendar's schedule flow) are still the pre-existing `setTimeout`/local-only mocks — none of them call the real `GenerateContentItem`/`GenerateVisualAsset`/`GenerateMarketingPlan`/`SchedulePost` endpoints yet (confirmed via a full grep — zero frontend references to any of these routes). The spend-preview caption added to `content-gen-page` is still accurate (it reads real backend-configured prices), but clicking "generate" today does not actually charge anything anywhere. Wiring these mock flows to their real, already-coin-metered endpoints remains the single most consequential next step — see "Suggested next module" below.
21. **The full campaign module — invite fixes, coin grants, and a real end-to-end AI campaign flow (research → diagnosis → strategy → refine → approve → content → schedule), plus real coin-charging content-gen/ads/calendar** (the largest single session to date — backend fully real and tested, frontend substantially rewired):
    - **Blocker fixes (Phase 1)**: Free plan's `MaxUsers` was `1` while the owner already occupied that seat, making every invite on Free fail outright — reseeded to `MaxUsers=2` (`SubscriptionPlanConfiguration`). New `SubscriptionPlan.MonthlyCoinGrant` (Free 100 / Plus 10,000 / Pro 30,000 / Ultra 80,000) + `Subscription.LastCoinGrantAt`; `ChangeSubscriptionPlanCommandHandler` now grants coins on a genuine plan change (captures the pre-mutation plan/period so re-selecting the same plan can never double-grant), writes a new `BillingTransaction.CoinGrant`, and returns `newCoinBalance`/`coinsGranted`. `AiCreditsPolicy`'s monthly-quota **enforcement** was retired at its one call site in `GenerateMarketingPlanCommandHandler` (coins are the real per-action meter now) — `GetAiCreditsUsageQuery` stays as a read-only billing-page stat. `billing-page.ts`'s `confirmPlanChange()`/`cancelSubscription()` now call `tenantService.refresh()` (previously missing, so the new coin balance/plan never rendered).
    - **Invite bug sweep (Phase 1c, ~8 fixes from a dedicated audit)**: `AddTeamMemberCommandHandler` now excludes `Declined` `TenantMember` rows and expired `TenantInvitation` rows from both the seat count and the "already a member/invited" checks (previously a single decline permanently burned a seat *and* blocked re-inviting that person forever); a new cross-check catches a stale pending `TenantInvitation` on the existing-user path so coins can't be escrowed twice; `AddTeamMemberCommandValidator` now rejects `AllocatedCoins > 0` for an Admin invite (that wallet was previously unspendable — `CoinPolicy` routes Admin+ spend straight from the pool); `AcceptInviteCommandHandler` now enforces the same 7-day expiry window as the hashed-token path (the `TenantMember`-kind invite link never expired before); `UpdateTeamMemberCommandHandler` now refunds `AllocatedCoins − SpentCoins` to the pool when promoting Editor/Viewer→Admin (the wallet was otherwise stranded) and writes the `team.role_changed`/`team.brand_access_changed` audit actions the frontend already had dead labels for. Frontend: `invite-login.ts`'s `emailMismatch()` now fails **closed** (blocks) instead of open while `currentUser()` is still resolving; `onRegistered()`/`acceptAsSignedInUser()` now also call `tenantService.refresh()` (not just `refreshMemberships()`, which no-ops when the active tenant is already valid).
    - **Campaign creation unblocked + schema for the real flow (Phase 2)**: `CreateCampaignCommandHandler`'s `plan.Cost <= 0` gate is gone entirely — every plan including Free (now `MaxCampaignsMonthly=1`) can create a campaign; it now also fires the previously-missing `NotificationPublisher.Notify`/`AuditLogger.Log` on creation. `MarketingCampaign` gained four nullable columns matching the existing `AiPlanJson` opaque-JSON convention: `BriefJson` (the onboarding wizard's answers — this is where that data finally lands server-side instead of only ever living in localStorage), `CompetitorResearchJson`, `DiagnosisJson`, `PlanApprovedAt` (the content-generation gate). `UpdateCampaignCommand` extended with `Objective`/`TargetPlatforms`/`BudgetCurrency`. One migration `AddCampaignEconomyAndFlow` covers all of Phase 1+2's schema; the one stale test asserting "Free plan rejects campaign creation" was repurposed into `Handle_WithFreePlan_AllowsCampaignCreation` + a new `Handle_WithFreePlanAtMonthlyLimit_RejectsCampaignCreation` (the cap itself, not the plan, is what's enforced now).
    - **New AI pipeline features, each following the existing coin-charge/AiJob/prompt-builder conventions exactly**: `Features/Campaigns/ResearchCampaignCompetitors/` (Tavily, query built from brand industry+keywords+brief positioning+location; writes one `RagDocument` per result **and** keeps Tavily's own `Answer` summary this time — unlike the pre-existing `AnalyzeCompetitorCommandHandler`, which discards it; charges `CompetitiveAnalysis` 3,500 **only when it actually finds data** — a Tavily failure/empty result is recorded as `{unavailable:true, note}` and the command still returns `Result.Success`, since competitor research is explicitly best-effort and must never block the flow). `Features/Campaigns/GenerateBusinessDiagnosis/` ("what we understood about your business" — SWOT/maturity/growth-stage/readiness/risks/opportunities/missing-info; charges `BusinessDiagnosis` 4,000). `ContentPromptBuilder.BuildCampaignStrategyPrompt` replaces the old thin `BuildMarketingPlanPrompt` shape (now `executiveSummary`/`businessAndMarketAnalysis`/`brandStrategy`/`marketingStrategy`/`campaignBlueprint`/`contentProductionPlan`/`executionRoadmap`/`aiRecommendations`), grounded in the brief + diagnosis with competitor research folded in as advisory-only ("may or may not be relevant, disregard where it isn't" — stated explicitly in the prompt text per the product requirement); still served by the existing `GenerateMarketingPlanCommandHandler`/12,000-coin/one-free-trial endpoint. New `Features/Campaigns/RefineCampaignPlan/` (free-text "عدّل الخطة", charges `ReasoningConversation` 2,500, blocked once approved) and `Features/Campaigns/ApproveCampaignPlan/` (stamps `PlanApprovedAt`, Draft→Active). `GenerateCampaignContentCommandHandler` now refuses to run unless `PlanApprovedAt` is set. Two small existing-endpoint fixes: `BuildOnboardingQuestionsPrompt` now asks for 3–8 questions instead of a hardcoded "exactly 5", and `GenerateOnboardingQuestionsCommandHandler` now actually charges `ReasoningConversation` coins (previously wired up but free).
    - **Per-post review + bulk scheduling fixes (Phase 5-6)**: `ReviewContentItemCommand` dropped from `Admin`-only (no brand-access check) to `Editor` + `IRequireResolvedBrandAccess`, matching every other content command. `RegenerateContentItemCommandHandler`/`ContentPromptBuilder.BuildRevisionPrompt` now allow standalone (non-campaign, `CampaignId is null`) content to be regenerated — previously hard-rejected. New `Features/Campaigns/ScheduleCampaignPosts/` bulk-schedules every Approved, not-yet-scheduled post in a campaign to one social account, reusing `SchedulePostCommandHandler`'s exact per-post checks (plan cap, coin cost, native-scheduling handoff) one at a time so one failure doesn't cost or block the rest — returns per-post success/failure.
    - **`ScheduledPostSummary`/`TopPostItem` enrichment**: both now carry the real post `Content` (joined from `ContentItem`) and `ImageUrl` (the specific `VisualAsset` attached at scheduling time / joined from `PostAnalyticsAggregation.LatestPostSnapshot`) — previously `AdService.toAd()`/`ScheduledPostService` had to fall back to the account name as a content stand-in. `TopPostItem`/`LatestPostSnapshot` also gained `CampaignId` so the dashboard's top-posts can link through to the real post-detail route. `GetCampaignAnalyticsQueryHandler` was scoped via `ScheduledPost.ContentItem.CampaignId`; realigned to the same denormalized `ScheduledPost.CampaignId` the Dashboard/GetScheduledPosts queries already use, so the two can no longer silently diverge.
    - **Frontend — the onboarding wizard becomes real campaign creation**: `RawajOnboarding` now resolves a real brand profile (defaults to the header's global selection; a compact chip picker appears at step 1 only when the tenant has more than one brand) and, once the step-7 chat finishes, calls the real `POST /campaigns` with the full onboarding blob as `BriefJson` before advancing to the approval step — replacing the old "write a mock plan to `localStorage['rawaj.plans']`" flow entirely. `onboarding-step-seven` now calls the real `POST /brand-profiles/{id}/onboarding-questions` (3–8 real AI questions, real coin charge) instead of 5 hardcoded questions behind a fake `setTimeout`. `onboarding-plan-approval` now runs the real pipeline on load (research → diagnosis → strategy, with a "never blocks" note if research comes back empty) instead of a fake 6-stage timer, renders the real diagnosis/competitor-research/strategy sections above the pre-existing deterministic weekly-calendar/budget/KPI preview (kept as-is — it's a legitimate, non-fake, real-onboarding-data-derived preview, not literal AI output), adds a real "عدّل الخطة" refine box and a real approve button that stamps `PlanApprovedAt` server-side before routing to the new content-review page. New `features/campaigns/campaign-content-page/` (route `/dashboard/campaigns/:id/content`): generate content (gated on `planApprovedAt`), per-post accept/refuse/regenerate-with-feedback, and a "جدولة الحملة" action that hard-blocks with **"حاليًا يمكن الجدولة على فيسبوك فقط — اربط صفحة فيسبوك أولًا"** when no active Facebook `SocialAccount` exists, otherwise calls the real bulk-schedule endpoint. Fixed `marketing-plan-page.ts:338`'s dead `/rawaj-onboarding` route reference (should always have been `/on-boarding`).
    - **`content-gen-page` is real now** (this session's explicit closing ask): `generate()`/`applyEdit()` call the real `POST /content-items/generate` (text) or `POST /visual-assets/generate` (images — no real video generation exists, so picking "video" now shows a clear "not available yet" message instead of faking one) and `POST /content-items/{id}/regenerate`, replacing the `setTimeout` + in-memory mock entirely; a new `MediaService.replaceId()` swaps the locally-assigned placeholder id for the real backend id once generation succeeds (so a later accept/regenerate targets the actual row); both success paths call `tenantService.refresh()`/`coinPricingService.refresh()` so the header's coin balance and free-trial-remaining counts update immediately, matching the pattern already established on `billing-page`.
    - **منشوراتك (`AdService`) and the calendar are real now**: `AdService.toAd()`/`ScheduledPostService` now show the real post copy and image (joined in via the `ScheduledPostSummary` enrichment above) instead of a hardcoded name/`format:'text'` stub. `ScheduledPostService.cancel()` (real `POST /scheduled-posts/{id}/cancel`) replaces the previously local-only `remove()` at both call sites (`calendar-page`, `campaign-post-detail-page`) — `remove()` now only mutates the local list and is called *after* `cancel()` succeeds. Both calendar pages' hardcoded `new Date(2026, 5, 4)` default now resolve to the selected campaign's real `startDate` (campaign-scoped calendar) or update live to whichever campaign the header's global selector switches to (general calendar), falling back to "today" for "all campaigns"/no date.
    - **Dashboard insights (Phase 9)**: `crm-page` gained an in-page campaign `<select>` switcher (previously relied solely on the header dropdown) that reads/writes the same `BrandContextService` state; its top-posts card links each post through to `/dashboard/campaigns/:id/posts/:postId` when the post has a campaign. `marketing-plan-page` — **this page's data source regressed to always-empty the moment the onboarding wizard stopped writing to `localStorage['rawaj.plans']` earlier in this same session**; fixed in the same pass by repointing it at real campaigns: it now fetches every campaign, keeps the ones with a `BriefJson`, and feeds that real (parsed) onboarding data into the exact same pre-existing deterministic calendar/budget/KPI renderer (`MpPlanDetail` needed zero changes — it only ever consumed a plain `OnboardingSnap`-shaped object). Its "approve"/edit actions now route to the real campaign content page / stay local-preview-only respectively, both clearly commented.
    - Backend: 49/49 tests pass (1 test repurposed for the Free-plan behavior flip, as noted above; no regressions across any of the 6 backend phases). `dotnet build`/`ng build` both clean throughout (only the same pre-existing bundle-budget warnings, zero new errors) after every phase.
    - **Known scope trim, explicit**: the extraction of the 4x-duplicated `requireBrandProfile()` inline pattern into a shared guard/service (a plan "nice to have") was **not** done — it's still copy-pasted per page, unchanged from before this session. No functional gap, just a refactor left on the table.

## What's real vs. still mock

- **Real (hits the live API)**: register, login, refresh-token, logout, tenant auto-provisioning + memberships + `X-Tenant-Id` switching, `GetMyTenant`, `UpdateTenantProfile`, `UpgradeToAgency`, social-account connect/list/disconnect (OAuth), **brand profiles** (list/create/archive + logo upload), **campaigns** (create/update/archive, plus the entire AI pipeline — research/diagnosis/strategy/refine/approve/generate-content/schedule, item 21), **team members / collaboration / coin wallets / email invites / audit log** (item 19), **the entire subscription/coin economy** — plans, coin packages, add-ons, fake-payment plan changes, billing history, coin pricing (item 20, both public/anonymous and tenant-scoped), welcome + invite emails, **the onboarding wizard** (real campaign creation + real AI chat + real approval pipeline, item 21), **content-gen-page** (real text/image generation + regenerate, item 21), **منشوراتك/`AdService`** (real content+image join, item 21), **calendar** (real cancel, campaign-date defaults, item 21), **dashboard** (`crm-page`'s in-page campaign switcher + linked top-posts, item 21).
- **Still mock-data-driven**: the per-brand social-connection UI in the Settings "connections" tab (separate from the real `/dashboard/social-accounts` page), and the entire admin dashboard (stats/users/tenants/plans/settings) — even though the backend already has real endpoints for admin (`GetPlatformStats`, `GetUsers`, `GetTenants`, `SetUserActive`). `ScheduledPostService.update()` (the calendar's edit-post modal) is still local-only — no backend "update a scheduled post" endpoint exists (only cancel/publish-now), so editing a scheduled post's time/content doesn't persist.

## Known issues / things to fix, not forget

- **No live end-to-end pass was run for item 21's full flow** in this session (register → brand profile → wizard → chat → research → diagnosis → strategy → refine → approve → generate content → accept/refuse/regenerate → connect Facebook → bulk schedule → verify calendar/منشوراتك/dashboard/notification, with running coin-balance checks at each stage) — `dotnet test` (49/49) and `ng build` (clean) both pass, and every individual phase was verified to compile/build correctly, but the full live click-through (which also needs real Groq/HuggingFace/Tavily API keys configured) has not been done. This is the natural first thing to verify in the next session.
- The `requireBrandProfile()` inline pattern is still duplicated across 5+ page files (campaigns-page, ads-page, calendar-page, marketing-plan-page, campaign-content-page) — never extracted into a shared guard/service, a known scope trim from item 21.
- `ScheduledPostService.update()` has no real backend endpoint to call yet (see above) — the calendar's "edit post" modal only mutates the local view.
- Nothing from the team-collaboration module (item 19), the subscription-economy module (item 20), or the campaign module (item 21) has been committed/pushed yet — per this project's established convention, only commit/branch/push when the user explicitly asks.
- No backend command exists to promote/demote a platform admin — only doable via direct SQL right now. Worth a real `SetPlatformAdmin` command if this needs to happen more than once.
- `preferredLanguage` is hardcoded to `'Ar'` at registration — no language picker in the sign-up UI yet.
- The backend's `ScheduledPostPublisherHostedService` background job fails repeatedly in the logs because `Encryption:Key` in `appsettings*.json` is still the placeholder `REPLACE_WITH_BASE64_ENCRYPTION_KEY` (not valid base64) — pre-existing, unrelated to this work, not fixed (out of scope).
- CORS is already fully open in `Program.cs` (`AllowAnyOrigin/AllowAnyMethod/AllowAnyHeader`) — no backend change needed there for local frontend↔backend calls.
- **LinkedIn OAuth is still a placeholder credential** in both `appsettings.json` and `appsettings.Development.json` (only Meta/Facebook has a real app configured) — the "ربط الحساب" button for LinkedIn on `/dashboard/social-accounts` will reach the redirect but fail on LinkedIn's side until a real app is registered.
- **No coin-spend hook exists yet.** Tenants are granted starting/reward coins, but nothing deducts them — the natural place is wherever AI content/campaign generation gets wired to real HTTP (content-gen is still mock), not this pass.
- The auth-module frontend changes were previously flagged as uncommitted directly on `dev` — since resolved (branched, committed, pushed as `feature/auth-module-real-backend-integration`); this session's tenant-lifecycle work should follow the same branch-and-push pattern.
- Items 12–15 (brand profiles → real HTTP, requirement gating, logo file storage, auth nav-link fixes) are **done in the working tree but not yet committed** — both `dotnet build` and `ng build` pass. Follow the same branch-and-push pattern before continuing.
- The brand-profile logo upload endpoint is standalone ("save file, return URL") and is **not** tied to any brand row, so abandoning the create wizard after uploading a logo leaves an orphan file under `wwwroot/media/brandprofiles/logourls/` — acceptable for now (matches how avatar uploads behave), but a cleanup pass could prune unreferenced logos later. The onboarding wizard still only records a logo *filename*, not a real uploaded file — wiring it to this same endpoint is a follow-up.

## Suggested next module

Brand profiles, campaigns (the full AI pipeline), team collaboration, the
subscription/coin economy, the onboarding wizard, content-gen, ads, and the
calendar are all real now (items 12–21) — this was the last major mock
surface on the tenant side. Two things worth doing before anything else:

1. **A real live end-to-end pass of item 21's full flow** on a fresh Free
   account (register → confirm the 50,000 dummy testing coin balance →
   brand profile → wizard → real campaign row with `BriefJson` → step-7 chat
   charges 2,500 → competitor research charges 3,500 (or gracefully shows
   "unavailable") → diagnosis charges 4,000 → strategy (free first campaign,
   else 12,000) → refine charges 2,500 → approve → generate content (200/400
   each, first 5 free) → accept/refuse/regenerate → connect Facebook → bulk
   schedule at 100 each → verify the results show up in the calendar,
   منشوراتك, and the campaign's dashboard insights, and that a "campaign
   created" notification fired. This needs real Groq/HuggingFace/Tavily API
   keys configured locally — hasn't been run this session (see "Known
   issues").
2. Runner-up: the Settings "connections" tab (agency per-brand social
   connections, separate from the real `/dashboard/social-accounts` page)
   and the entire admin dashboard (stats/users/tenants/plans/settings) are
   still mock, even though the backend already has real endpoints for the
   latter (`GetPlatformStats`, `GetUsers`, `GetTenants`, `SetUserActive`).
